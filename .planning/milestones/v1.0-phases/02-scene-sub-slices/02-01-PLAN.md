---
phase: 02-scene-sub-slices
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/stores/slices/scene/geometrySubSlice.ts
  - frontend/src/stores/slices/scene/selectionSubSlice.ts
  - frontend/src/stores/slices/scene/editingSubSlice.ts
  - frontend/src/stores/slices/scene/drawingSubSlice.ts
  - frontend/src/stores/slices/scene/index.ts
autonomous: true
requirements: [SCEN-01, SCEN-02, SCEN-03, SCEN-04, SCEN-05, SCEN-06, SCEN-07]

must_haves:
  truths:
    - "Four sub-slice files exist with correct interface + creator exports"
    - "Barrel index.ts composes SceneSlice from sub-slice intersection types"
    - "createSceneSlice in barrel spreads all four sub-slice creators"
    - "Camera state lives in geometry sub-slice (locked decision)"
    - "Mode field and setMode live in geometry sub-slice (locked decision)"
    - "Helper functions getActiveCurves and selectPreferredCurve live in geometrySubSlice and are re-exported from barrel"
  artifacts:
    - path: "frontend/src/stores/slices/scene/geometrySubSlice.ts"
      provides: "GeometrySubSlice interface + createGeometrySubSlice + helpers"
      exports: ["GeometrySubSlice", "createGeometrySubSlice", "getActiveCurves", "selectPreferredCurve"]
    - path: "frontend/src/stores/slices/scene/selectionSubSlice.ts"
      provides: "SelectionSubSlice interface + createSelectionSubSlice"
      exports: ["SelectionSubSlice", "createSelectionSubSlice"]
    - path: "frontend/src/stores/slices/scene/editingSubSlice.ts"
      provides: "EditingSubSlice interface + createEditingSubSlice"
      exports: ["EditingSubSlice", "createEditingSubSlice"]
    - path: "frontend/src/stores/slices/scene/drawingSubSlice.ts"
      provides: "DrawingSubSlice interface + createDrawingSubSlice"
      exports: ["DrawingSubSlice", "createDrawingSubSlice"]
    - path: "frontend/src/stores/slices/scene/index.ts"
      provides: "Composed SceneSlice type + createSceneSlice + helper re-exports"
      exports: ["SceneSlice", "createSceneSlice", "getActiveCurves", "selectPreferredCurve"]
  key_links:
    - from: "frontend/src/stores/slices/scene/index.ts"
      to: "frontend/src/stores/slices/scene/geometrySubSlice.ts"
      via: "import + spread in createSceneSlice"
      pattern: "createGeometrySubSlice\\(set, get, store\\)"
    - from: "frontend/src/stores/slices/scene/index.ts"
      to: "frontend/src/stores/slices/scene/selectionSubSlice.ts"
      via: "import + spread in createSceneSlice"
      pattern: "createSelectionSubSlice\\(set, get, store\\)"
    - from: "frontend/src/stores/slices/scene/index.ts"
      to: "frontend/src/stores/slices/scene/editingSubSlice.ts"
      via: "import + spread in createSceneSlice"
      pattern: "createEditingSubSlice\\(set, get, store\\)"
    - from: "frontend/src/stores/slices/scene/index.ts"
      to: "frontend/src/stores/slices/scene/drawingSubSlice.ts"
      via: "import + spread in createSceneSlice"
      pattern: "createDrawingSubSlice\\(set, get, store\\)"
    - from: "frontend/src/stores/slices/scene/drawingSubSlice.ts"
      to: "frontend/src/stores/slices/scene/geometrySubSlice.ts"
      via: "import helpers"
      pattern: "import.*getActiveCurves.*selectPreferredCurve.*from.*geometrySubSlice"
---

<objective>
Create the four sub-slice files and barrel for the scene slice decomposition.

Purpose: Extract all ~60 SceneSlice members from the monolithic sceneSlice.ts into four focused sub-slice files (geometry, selection, editing, drawing) plus a barrel index.ts that composes them back. The original sceneSlice.ts is NOT deleted in this plan -- it will be replaced in Plan 02.

Output: Five new files in `frontend/src/stores/slices/scene/` that together export the same `SceneSlice` type and `createSceneSlice` function.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scene-sub-slices/02-CONTEXT.md
@.planning/phases/02-scene-sub-slices/02-RESEARCH.md

<interfaces>
<!-- The existing SceneSlice interface and createSceneSlice are in sceneSlice.ts (596 lines).
     The executor MUST read this file in full to extract members. -->

From frontend/src/store.tsx (lines 1-17):
```typescript
import type { SceneSlice } from "./stores/slices/sceneSlice";
import { createSceneSlice } from "./stores/slices/sceneSlice";

export {
  getActiveCurves,
  selectPreferredCurve,
} from "./stores/slices/sceneSlice";

export type AppState = ConnectionSlice & PlaybackSlice & SceneSlice & LockSlice & UISlice;
```

From frontend/src/stores/slices/sceneSlice.ts (line 1-12, imports):
```typescript
import type * as THREE from "three";
import type { StateCreator } from "zustand";
import {
  acquireEditLock,
  createGeometry,
  getGeometry,
  partialUpdateFrame,
  releaseEditLock,
  updateActiveCamera,
  updateSelection as updateSelectionAPI,
} from "../../myapi/client";
import type { AppState } from "../../store";
```

From frontend/src/stores/slices/connectionSlice.ts (StateCreator pattern):
```typescript
export const createConnectionSlice: StateCreator<AppState, [], [], ConnectionSlice> = (set) => ({
  // ... state + actions
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create geometry and selection sub-slices</name>
  <files>
    frontend/src/stores/slices/scene/geometrySubSlice.ts
    frontend/src/stores/slices/scene/selectionSubSlice.ts
  </files>
  <action>
Read `frontend/src/stores/slices/sceneSlice.ts` in full. Create the `frontend/src/stores/slices/scene/` directory.

**geometrySubSlice.ts** -- Create with:

Imports:
- `import type * as THREE from "three";`
- `import type { StateCreator } from "zustand";`
- `import { createGeometry, getGeometry, updateActiveCamera } from "../../../myapi/client";`
- `import type { AppState } from "../../../store";`

Module-level helper functions (move from sceneSlice.ts lines 15-25):
- `getActiveCurves` (exported)
- `selectPreferredCurve` (exported)

`GeometrySubSlice` interface with these members (per RESEARCH member classification):

State fields:
- `geometries: Record<string, any>`
- `geometrySchemas: Record<string, any>`
- `geometryDefaults: Record<string, any>`
- `geometryUpdateSources: Record<string, "local" | "remote">`
- `geometryFetchingStates: Record<string, boolean>`
- `neededFrameKeys: Record<string, string[]>`
- `mode: "view" | "drawing" | "editing"` (locked decision: geometry is "base" sub-slice)
- `attachedCameraKey: string | null` (locked decision: camera merges into geometry)
- `curveRefs: Record<string, THREE.Curve<THREE.Vector3>>`
- `hoveredGeometryInstance: { geometryKey: string; instanceId: number } | null`
- `particleCount: number`
- `curveLength: number`
- `loadedDynamicPositions: Map<string, { positionKey: string; positions: Float32Array }>`

Actions:
- `setGeometries`, `setGeometrySchemas`, `setGeometryDefaults`, `updateGeometry`, `removeGeometry`
- `setGeometryFetching`, `removeGeometryFetching`, `registerFrameKeys`, `unregisterFrameKeys`, `getIsFetching`
- `setMode` (locked decision)
- `attachToCamera`, `setAttachedCameraKey` (locked decision)
- `registerCurveRef`, `unregisterCurveRef`
- `setHoveredGeometryInstance`
- `setParticleCount`, `setCurveLength`
- `registerLoadedDynamicPositions`, `unregisterLoadedDynamicPositions`

`createGeometrySubSlice` function:
- Signature: `StateCreator<AppState, [], [], GeometrySubSlice>`
- Copy implementations verbatim from sceneSlice.ts for each action
- `removeGeometry` uses `getActiveCurves` and `selectPreferredCurve` (local imports, same file)
- `removeGeometry` accesses cross-slice state (`selections`, `activeCurveForDrawing`, `editingCallbacks`) via `set((state) => ...)` -- this is correct because `state` is full `AppState`
- `attachToCamera` accesses `roomId` and `sessionId` from ConnectionSlice via `get()`
- `getIsFetching` accesses `geometries` and `geometryFetchingStates` from own state via `get()`

CRITICAL: Preserve the `editingCallbacks` Map copy in `removeGeometry` exactly as-is (`new Map(state.editingCallbacks)` then `.delete(key)`).

**selectionSubSlice.ts** -- Create with:

Imports:
- `import type { StateCreator } from "zustand";`
- `import { updateSelection as updateSelectionAPI } from "../../../myapi/client";`
- `import type { AppState } from "../../../store";`

`SelectionSubSlice` interface with these members:

State fields:
- `selections: Record<string, number[]>`
- `selectionGroups: Record<string, Record<string, number[]>>`

Actions:
- `setSelections`, `updateSelectionForGeometry`, `setSelectionGroups`, `loadSelectionGroup`, `updateSelections`

`createSelectionSubSlice` function:
- Signature: `StateCreator<AppState, [], [], SelectionSubSlice>`
- Copy implementations verbatim from sceneSlice.ts
- `updateSelectionForGeometry` accesses `roomId` from ConnectionSlice via `get()`
- `loadSelectionGroup` calls `get().updateSelectionForGeometry()` (cross-action call within same conceptual domain)
- `updateSelections` accesses `roomId`, `selections` via `get()`

Use `import type { AppState }` (type-only) in both files to avoid circular import issues at runtime.
  </action>
  <verify>
    <automated>ls -la frontend/src/stores/slices/scene/geometrySubSlice.ts frontend/src/stores/slices/scene/selectionSubSlice.ts && echo "Files exist"</automated>
  </verify>
  <done>
    - geometrySubSlice.ts exports GeometrySubSlice interface, createGeometrySubSlice, getActiveCurves, selectPreferredCurve
    - selectionSubSlice.ts exports SelectionSubSlice interface, createSelectionSubSlice
    - Camera state (attachedCameraKey, attachToCamera, setAttachedCameraKey) is in geometry sub-slice
    - Mode field and setMode are in geometry sub-slice
    - All action implementations are verbatim copies from sceneSlice.ts (no functional changes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create editing and drawing sub-slices plus barrel</name>
  <files>
    frontend/src/stores/slices/scene/editingSubSlice.ts
    frontend/src/stores/slices/scene/drawingSubSlice.ts
    frontend/src/stores/slices/scene/index.ts
  </files>
  <action>
Read `frontend/src/stores/slices/sceneSlice.ts` in full (for copying implementations).

**editingSubSlice.ts** -- Create with:

Imports:
- `import type * as THREE from "three";`
- `import type { StateCreator } from "zustand";`
- `import { acquireEditLock, partialUpdateFrame, releaseEditLock } from "../../../myapi/client";`
- `import type { AppState } from "../../../store";`

Note: `acquireEditLock` and `releaseEditLock` are NOT directly called in editing sub-slice actions. The editing actions call `get().acquireLock()` and `get().releaseLock()` from LockSlice. However, check the original code -- if `acquireEditLock`/`releaseEditLock` are only imported at the sceneSlice.ts top level for use in lock-related calls, they do NOT need to be imported in editingSubSlice. Only import what is actually used directly. `partialUpdateFrame` IS used in `saveFrameEdits` and `setPendingFrameEdit`.

`EditingSubSlice` interface with these members:

State fields:
- `transformMode: "translate" | "rotate" | "scale"`
- `editingSelectedAxis: "x" | "y" | "z" | null`
- `editingCombinedCentroid: [number, number, number] | null`
- `editingCallbacks: Map<string, Set<(matrix: THREE.Matrix4) => void>>`
- `pendingFrameEdits: { frameId: number; keys: Record<string, any> } | null`
- `editingFrameDataCount: number`

Actions:
- `enterEditingMode`, `exitEditingMode` (locked decision: live in editing sub-slice)
- `setTransformMode`, `cycleTransformMode`, `setEditingSelectedAxis`
- `setEditingCombinedCentroid`
- `subscribeToEditing`, `notifyEditingChange`
- `setPendingFrameEdit`, `clearPendingFrameEdits`, `saveFrameEdits`
- `incrementEditingFrameDataCount`, `decrementEditingFrameDataCount`

`createEditingSubSlice` function:
- Signature: `StateCreator<AppState, [], [], EditingSubSlice>`
- Copy implementations verbatim from sceneSlice.ts
- `enterEditingMode` accesses `mode` via `get()` (geometry sub-slice owns mode), calls `get().acquireLock()` (lock slice), calls `state.showSnackbar()` (UI slice)
- `exitEditingMode` accesses `mode`, `selections` via `get()`, calls `get().saveFrameEdits()` (self), `get().updateSelectionForGeometry()` (selection sub-slice), `get().releaseLock()` (lock slice), `state.showSnackbar()` (UI slice)
- `cycleTransformMode` calls `get().saveFrameEdits()` (self) and `get().showSnackbar()` (UI slice)
- `saveFrameEdits` accesses `roomId` via `get()` (connection slice), calls `partialUpdateFrame` API
- `setPendingFrameEdit` accesses `roomId` via `get()`, calls `partialUpdateFrame` API, calls `showSnackbar` via `get()`

**drawingSubSlice.ts** -- Create with:

Imports:
- `import type * as THREE from "three";`
- `import type { StateCreator } from "zustand";`
- `import { createGeometry, getGeometry } from "../../../myapi/client";`
- `import type { AppState } from "../../../store";`
- `import { getActiveCurves, selectPreferredCurve } from "./geometrySubSlice";`

`DrawingSubSlice` interface with these members:

State fields:
- `drawingPointerPosition: THREE.Vector3 | null`
- `drawingIsValid: boolean`
- `activeCurveForDrawing: string | null`

Actions:
- `enterDrawingMode`, `exitDrawingMode` (locked decision: live in drawing sub-slice)
- `setDrawingPointerPosition`, `setDrawingIsValid`, `setActiveCurveForDrawing`

`createDrawingSubSlice` function:
- Signature: `StateCreator<AppState, [], [], DrawingSubSlice>`
- Copy implementations verbatim from sceneSlice.ts
- `enterDrawingMode` accesses `geometries`, `mode`, `roomId` via `get()`, calls `get().acquireLock()` (lock slice), uses `getActiveCurves` and `selectPreferredCurve` (imported from geometrySubSlice)
- `exitDrawingMode` accesses `mode` via `get()`, calls `get().releaseLock()` (lock slice)

CRITICAL: `enterDrawingMode` has a `queryClient?: any` parameter -- preserve this exactly.

**scene/index.ts (barrel)** -- Create with:

```typescript
import type { StateCreator } from "zustand";
import type { AppState } from "../../../store";

import type { DrawingSubSlice } from "./drawingSubSlice";
import { createDrawingSubSlice } from "./drawingSubSlice";
import type { EditingSubSlice } from "./editingSubSlice";
import { createEditingSubSlice } from "./editingSubSlice";
import type { GeometrySubSlice } from "./geometrySubSlice";
import { createGeometrySubSlice } from "./geometrySubSlice";
import type { SelectionSubSlice } from "./selectionSubSlice";
import { createSelectionSubSlice } from "./selectionSubSlice";

export type SceneSlice = GeometrySubSlice &
  SelectionSubSlice &
  EditingSubSlice &
  DrawingSubSlice;

export const createSceneSlice: StateCreator<AppState, [], [], SceneSlice> = (
  set,
  get,
  store,
) => ({
  ...createGeometrySubSlice(set, get, store),
  ...createSelectionSubSlice(set, get, store),
  ...createEditingSubSlice(set, get, store),
  ...createDrawingSubSlice(set, get, store),
});

export { getActiveCurves, selectPreferredCurve } from "./geometrySubSlice";
```

CRITICAL: Pass all three arguments `(set, get, store)` to each sub-slice creator -- not just `(set, get)`.

Re-export sub-slice types if needed but the primary exports are `SceneSlice`, `createSceneSlice`, `getActiveCurves`, `selectPreferredCurve`.

After creating all files, run `cd frontend && bunx tsc --noEmit` to check for type errors. The old sceneSlice.ts still exists at this point, so store.tsx imports from there. Type errors in the new files themselves (if any) should be fixed. The barrel and sub-slices must compile independently -- they only reference `AppState` (via type import) and each other.

Note: Since store.tsx still imports from the old sceneSlice.ts, the new scene/ directory files are not yet wired into the application. TypeScript may show errors in the new files if there are issues, but the overall build should still pass via the old path.
  </action>
  <verify>
    <automated>ls -la frontend/src/stores/slices/scene/editingSubSlice.ts frontend/src/stores/slices/scene/drawingSubSlice.ts frontend/src/stores/slices/scene/index.ts && echo "All 5 scene files exist"</automated>
  </verify>
  <done>
    - editingSubSlice.ts exports EditingSubSlice interface, createEditingSubSlice
    - drawingSubSlice.ts exports DrawingSubSlice interface, createDrawingSubSlice
    - enterEditingMode/exitEditingMode are in editing sub-slice (locked decision)
    - enterDrawingMode/exitDrawingMode are in drawing sub-slice (locked decision)
    - Drawing sub-slice imports getActiveCurves/selectPreferredCurve from geometrySubSlice
    - scene/index.ts exports composed SceneSlice type, createSceneSlice (spreads all 4), and re-exports helpers
    - createSceneSlice passes (set, get, store) to all sub-slice creators
    - All action implementations are verbatim copies from sceneSlice.ts (no functional changes)
  </done>
</task>

</tasks>

<verification>
All five files exist in `frontend/src/stores/slices/scene/`:
- `geometrySubSlice.ts` with ~25 members + 2 helpers
- `selectionSubSlice.ts` with ~8 members
- `editingSubSlice.ts` with ~15 members
- `drawingSubSlice.ts` with ~7 members
- `index.ts` barrel with composed type + creator + re-exports

The member count across all four sub-slices should equal the original SceneSlice interface member count (~60).
</verification>

<success_criteria>
- Four sub-slice files created with correct interface exports and creator functions
- Barrel index.ts composes SceneSlice = GeometrySubSlice & SelectionSubSlice & EditingSubSlice & DrawingSubSlice
- createSceneSlice in barrel spreads all four sub-slice creators with (set, get, store)
- Camera state in geometry sub-slice (locked)
- Mode field in geometry sub-slice (locked)
- Mode transitions in their respective sub-slices (locked)
- Helper functions in geometrySubSlice, re-exported from barrel
- All implementations are verbatim copies -- zero functional changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-scene-sub-slices/02-01-SUMMARY.md`
</output>
