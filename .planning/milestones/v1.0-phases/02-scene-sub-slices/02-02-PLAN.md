---
phase: 02-scene-sub-slices
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/store.tsx
  - frontend/src/stores/slices/sceneSlice.ts
autonomous: true
requirements: [SCEN-06, SCEN-07]

must_haves:
  truths:
    - "store.tsx imports SceneSlice and createSceneSlice from the new scene/ barrel"
    - "store.tsx re-exports getActiveCurves and selectPreferredCurve from the new scene/ barrel"
    - "The original monolithic sceneSlice.ts is deleted"
    - "TypeScript compilation passes with zero errors"
    - "All existing Playwright E2E specs pass unchanged"
  artifacts:
    - path: "frontend/src/store.tsx"
      provides: "Updated import paths pointing to scene/ barrel"
      contains: "from \"./stores/slices/scene\""
    - path: "frontend/src/stores/slices/sceneSlice.ts"
      provides: "MUST NOT EXIST (deleted)"
  key_links:
    - from: "frontend/src/store.tsx"
      to: "frontend/src/stores/slices/scene/index.ts"
      via: "import SceneSlice type and createSceneSlice"
      pattern: "import.*SceneSlice.*from.*stores/slices/scene"
    - from: "frontend/src/store.tsx"
      to: "frontend/src/stores/slices/scene/index.ts"
      via: "re-export helpers"
      pattern: "export.*getActiveCurves.*selectPreferredCurve.*from.*stores/slices/scene"
---

<objective>
Wire store.tsx to the new scene/ barrel, delete the monolithic sceneSlice.ts, and verify the refactoring is complete.

Purpose: Complete the structural refactor by switching the application from the old monolithic file to the new sub-slice composition. This is the "flip the switch" plan.

Output: Updated store.tsx with new import paths, deleted sceneSlice.ts, verified TypeScript compilation and E2E regression suite.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scene-sub-slices/02-CONTEXT.md
@.planning/phases/02-scene-sub-slices/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01's barrel (scene/index.ts) -->
From frontend/src/stores/slices/scene/index.ts:
```typescript
export type SceneSlice = GeometrySubSlice &
  SelectionSubSlice &
  EditingSubSlice &
  DrawingSubSlice;

export const createSceneSlice: StateCreator<AppState, [], [], SceneSlice> = (
  set, get, store,
) => ({
  ...createGeometrySubSlice(set, get, store),
  ...createSelectionSubSlice(set, get, store),
  ...createEditingSubSlice(set, get, store),
  ...createDrawingSubSlice(set, get, store),
});

export { getActiveCurves, selectPreferredCurve } from "./geometrySubSlice";
```

From frontend/src/store.tsx (current state, to be modified):
```typescript
import type { SceneSlice } from "./stores/slices/sceneSlice";
import { createSceneSlice } from "./stores/slices/sceneSlice";

export {
  getActiveCurves,
  selectPreferredCurve,
} from "./stores/slices/sceneSlice";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire store.tsx to scene barrel and delete old file</name>
  <files>
    frontend/src/store.tsx
    frontend/src/stores/slices/sceneSlice.ts
  </files>
  <action>
Read `frontend/src/store.tsx`. Make exactly these changes:

1. **Update SceneSlice import** (line 8):
   Change: `import type { SceneSlice } from "./stores/slices/sceneSlice";`
   To: `import type { SceneSlice } from "./stores/slices/scene";`

2. **Update createSceneSlice import** (line 9):
   Change: `import { createSceneSlice } from "./stores/slices/sceneSlice";`
   To: `import { createSceneSlice } from "./stores/slices/scene";`

3. **Update helper re-exports** (lines 14-17):
   Change: `} from "./stores/slices/sceneSlice";`
   To: `} from "./stores/slices/scene";`

Everything else in store.tsx MUST remain unchanged:
- `AppState` type composition stays the same
- `useAppStore` creation stays the same
- `Progress`, `InitializationError` interfaces stay the same
- Selectors `selectIsRoomReadOnly`, `selectActiveSelectionGroup` stay the same

After updating store.tsx, delete `frontend/src/stores/slices/sceneSlice.ts`.

Then run:
1. `cd frontend && bunx tsc --noEmit` -- must pass with zero errors
2. `cd frontend && bun run format` -- format any changed files
3. `cd frontend && bun run lint` -- ensure no lint issues

If `tsc --noEmit` fails, read the errors carefully. Common issues:
- Missing member in a sub-slice interface (fix by adding the member)
- Type signature mismatch (fix by matching original exactly)
- Import path issue (fix the path)
- Circular import (ensure all AppState imports use `import type`)

Do NOT proceed until TypeScript compiles cleanly.
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/zndraw-fastapi/frontend && bunx tsc --noEmit && echo "TypeScript compilation passed" && test ! -f src/stores/slices/sceneSlice.ts && echo "Old sceneSlice.ts deleted"</automated>
  </verify>
  <done>
    - store.tsx imports SceneSlice and createSceneSlice from "./stores/slices/scene"
    - store.tsx re-exports getActiveCurves and selectPreferredCurve from "./stores/slices/scene"
    - sceneSlice.ts is deleted
    - TypeScript compilation passes with zero errors
    - AppState type, useAppStore creation, selectors, and other store.tsx content unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Run E2E regression suite</name>
  <files></files>
  <action>
Run the full Playwright E2E test suite to verify no behavioral regressions:

```bash
cd frontend && bunx playwright test
```

This runs all 12 E2E spec files. All must pass unchanged since this is a pure structural refactor with no functional changes.

If any spec fails:
1. Read the failure output carefully
2. The failure is almost certainly due to a member being misplaced, missing, or having a different signature in the sub-slices
3. Compare the failing behavior against the original sceneSlice.ts implementation
4. Fix the sub-slice file(s) and re-run TypeScript compilation before re-running Playwright

Key specs to watch:
- `editing.spec.ts` -- exercises enterEditingMode/exitEditingMode, transform modes
- `geometry-drawing.spec.ts` -- exercises enterDrawingMode/exitDrawingMode, curve creation
- `camera-session.spec.ts` -- exercises camera attachment

If Playwright is not installed locally, run `cd frontend && bunx playwright install chromium` first, then `bunx playwright test --project=chromium` for a faster single-browser run.

Note: The `docs-screenshots.spec.ts` may require specific server state -- if it fails in isolation while all other specs pass, that is acceptable (it is a documentation utility, not a functional test).
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/zndraw-fastapi/frontend && bunx playwright test --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All existing Playwright E2E specs pass (12 spec files, all tests green)
    - No behavioral regressions from the structural refactor
    - The scene sub-slice decomposition is complete and verified end-to-end
  </done>
</task>

</tasks>

<verification>
1. `frontend/src/stores/slices/sceneSlice.ts` does NOT exist
2. `frontend/src/store.tsx` imports from `./stores/slices/scene` (not `./stores/slices/sceneSlice`)
3. `cd frontend && bunx tsc --noEmit` passes
4. `cd frontend && bunx playwright test` passes (all specs green)
5. No other files outside of store.tsx were modified (components still use `useAppStore` unchanged)
</verification>

<success_criteria>
- store.tsx wired to scene/ barrel with 3 import path changes
- Old sceneSlice.ts deleted
- TypeScript compiles with zero errors
- All Playwright E2E specs pass unchanged
- Phase 2 complete: monolithic sceneSlice.ts decomposed into 4 sub-slices
</success_criteria>

<output>
After completion, create `.planning/phases/02-scene-sub-slices/02-02-SUMMARY.md`
</output>
