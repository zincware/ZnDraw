---
phase: 03-socket-handler-modules
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - frontend/src/hooks/socketHandlers/connectionHandlers.ts
  - frontend/src/hooks/socketHandlers/frameHandlers.ts
  - frontend/src/hooks/socketHandlers/geometryHandlers.ts
  - frontend/src/hooks/socketHandlers/roomHandlers.ts
autonomous: true
requirements: [SOCK-01, SOCK-02, SOCK-03, SOCK-06, SOCK-08]

must_haves:
  truths:
    - "Connection handler includes handleRoomJoin as a separate internal function"
    - "Retry state (retryDelay, backoff constants) is owned by the connection module"
    - "Frame handler preserves the complex query predicate invalidation logic"
    - "Geometry handler uses createInvalidateHandler from utils for selections and bookmarks"
    - "Room handlers correctly interact with useRoomsStore for room_update and room_delete"
    - "All handler event parameters use typed interfaces instead of any"
  artifacts:
    - path: "frontend/src/hooks/socketHandlers/connectionHandlers.ts"
      provides: "Connection/lifecycle handler factory"
      exports: ["createConnectionHandlers", "RoomJoinResponse", "RoomJoinError"]
    - path: "frontend/src/hooks/socketHandlers/frameHandlers.ts"
      provides: "Frame handler factory"
      exports: ["createFrameHandlers", "FrameUpdateEvent", "FramesInvalidateEvent", "FrameSelectionUpdateEvent"]
    - path: "frontend/src/hooks/socketHandlers/geometryHandlers.ts"
      provides: "Geometry handler factory"
      exports: ["createGeometryHandlers", "GeometryInvalidateEvent", "DefaultCameraInvalidateEvent", "ActiveCameraUpdateEvent"]
    - path: "frontend/src/hooks/socketHandlers/roomHandlers.ts"
      provides: "Room/lock/progress handler factory"
      exports: ["createRoomHandlers", "RoomUpdateEvent", "RoomDeleteEvent", "LockUpdateEvent", "ProgressStartedEvent", "ProgressUpdateEvent", "ProgressCompleteEvent"]
  key_links:
    - from: "frontend/src/hooks/socketHandlers/connectionHandlers.ts"
      to: "frontend/src/myapi/client"
      via: "import createRoom, listGeometries, etc."
      pattern: "import.*from.*myapi/client"
    - from: "frontend/src/hooks/socketHandlers/connectionHandlers.ts"
      to: "frontend/src/socket.ts"
      via: "import socket singleton for emit calls"
      pattern: "import.*socket.*from.*socket"
    - from: "frontend/src/hooks/socketHandlers/geometryHandlers.ts"
      to: "frontend/src/hooks/socketHandlers/utils.ts"
      via: "import createInvalidateHandler"
      pattern: "import.*createInvalidateHandler.*from.*utils"
    - from: "frontend/src/hooks/socketHandlers/roomHandlers.ts"
      to: "frontend/src/roomsStore"
      via: "import useRoomsStore for getState()"
      pattern: "useRoomsStore\\.getState\\(\\)"
---

<objective>
Extract the four larger handler modules: connectionHandlers (largest, ~270 lines with handleRoomJoin), frameHandlers, geometryHandlers, and roomHandlers.

Purpose: Complete the extraction of all 7 handler modules. These are the more complex modules that handle connection lifecycle, frame invalidation with query predicates, geometry CRUD with cache management, and room/lock/progress state.

Output: 4 new files under `frontend/src/hooks/socketHandlers/`
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-socket-handler-modules/03-CONTEXT.md
@.planning/phases/03-socket-handler-modules/03-RESEARCH.md
@.planning/phases/03-socket-handler-modules/03-01-SUMMARY.md
@frontend/src/hooks/useSocketManager.ts
@frontend/src/hooks/socketHandlers/types.ts
@frontend/src/hooks/socketHandlers/utils.ts
@frontend/src/socket.ts
@frontend/src/store.tsx
@frontend/src/roomsStore.tsx
@frontend/src/myapi/client.ts

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/hooks/socketHandlers/types.ts:
```typescript
export interface HandlerContext {
  roomId: string | undefined;
  appStoreRoomId: string | null;
  isOverview: boolean;
  isCancelled: () => boolean;
  queryClient: QueryClient;
  // ... all ~30 fields
}
```

From frontend/src/hooks/socketHandlers/utils.ts:
```typescript
export function createInvalidateHandler<T>(
  fetchFn: (roomId: string) => Promise<T>,
  updateStoreFn: (data: T) => void,
  eventName: string,
  getRoomId: () => string | undefined,
): (data: unknown) => Promise<void>;
```

From frontend/src/socket.ts:
```typescript
export const socket: Socket;
export async function connectWithAuth(): Promise<AuthResult>;
```

From frontend/src/myapi/client.ts:
```typescript
export function createRoom(params: { room_id: string; copy_from?: string }): Promise<...>;
export function listGeometries(roomId: string): Promise<{ items: Record<string, GeometryData>; types: { schemas: ...; defaults: ... } }>;
export function getGeometry(roomId: string, key: string): Promise<{ geometry: GeometryData }>;
export function listSelectionGroups(roomId: string): Promise<{ items: Record<string, Record<string, number[]>> }>;
export function getAllBookmarks(roomId: string): Promise<{ items: Record<number, string> }>;
export function getFrameSelection(roomId: string): Promise<{ frame_selection: number[] | null }>;
export function getEditLockStatus(roomId: string): Promise<{ locked: boolean; sid?: string; user_id?: string; lock_token?: string; msg?: string; ttl?: number }>;
export function getServerVersion(): Promise<{ version: string }>;
export function getGlobalSettings(): Promise<GlobalSettings>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract connectionHandlers and frameHandlers</name>
  <files>frontend/src/hooks/socketHandlers/connectionHandlers.ts, frontend/src/hooks/socketHandlers/frameHandlers.ts</files>
  <action>
**connectionHandlers.ts** (from useSocketManager.ts lines 104-340, 798-819) -- the largest module (~270 lines):

Export typed interfaces:
- `RoomJoinResponse`: `{ session_id: string; step: number; frame_count: number; camera_key: string | null; locked: boolean; progress_trackers?: Record<string, Progress> }`
- `RoomJoinError`: `{ status: number; detail?: string }`

`createConnectionHandlers(ctx: HandlerContext)` returns `{ onConnect, onDisconnect, onConnectError }`:

- Owns retry state internally: `INITIAL_RETRY_DELAY = 1000`, `MAX_RETRY_DELAY = 30000`, `let retryDelay = INITIAL_RETRY_DELAY`
- Extract `handleRoomJoin` as a separate named function WITHIN the factory closure (per user decision). This contains the bulk of onConnect logic (lines 161-287): session setup, geometry fetching, selection extraction, secondary data loading (selection groups, bookmarks, frame selection, edit lock status)
- `onConnect` -- resets retryDelay, clears initialization error, fetches server version and global settings, checks version compatibility, then handles overview vs room join. For room join: emits `room_join` via `socket.emit()`, handles 404 by creating room via REST then retrying join. Calls `handleRoomJoin` for success processing
- `onDisconnect` -- calls `ctx.setConnected(false)`. Does NOT clear sessionId (per existing comment)
- `onConnectError(err: Error)` -- checks `ctx.isCancelled()`, handles auth errors with backoff retry via `connectWithAuth()`

Imports needed from other modules (stateless utils, NOT via HandlerContext):
- `socket`, `connectWithAuth` from `../../socket`
- `createRoom`, `listGeometries`, `listSelectionGroups`, `getAllBookmarks`, `getFrameSelection`, `getEditLockStatus`, `getServerVersion`, `getGlobalSettings` from `../../myapi/client`
- `logout` from `../../utils/auth`
- `setLastVisitedRoom` from `../../utils/roomTracking`
- `checkVersionCompatibility`, `getClientVersion` from `../../utils/versionCompatibility`
- `useAppStore` from `../../store` (for `.getState()` imperative reads ONLY -- showSnackbar, resetChatUnread, sessionId, lockToken, startLockRenewal, startLockExpiryTimer, setState)

Preserve ALL existing behavior exactly. The `cancelled` check uses `ctx.isCancelled()` instead of the local `cancelled` variable.

**frameHandlers.ts** (from useSocketManager.ts lines 348-456):

Export typed interfaces:
- `FrameUpdateEvent`: `{ frame: number }`
- `FramesInvalidateEvent`: `{ room_id: string; action: "add" | "delete" | "modify" | "clear"; indices?: number[]; count?: number | null; reason?: string | null }`
- `FrameSelectionUpdateEvent`: `{ indices: number[] | null }`

`createFrameHandlers(ctx: HandlerContext)` returns `{ onFrameUpdate, onFramesInvalidate, onFrameSelectionUpdate }`:

- `onFrameUpdate(data: FrameUpdateEvent)` -- reads `useAppStore.getState().playing` (imperative, NOT from context). If playing, ignores the event. Otherwise calls `ctx.setCurrentFrame(data.frame)`
- `onFramesInvalidate(data: FramesInvalidateEvent)` -- updates frameCount if `count != null`, shows snackbar on provider_disconnected, invalidates React Query cache using the predicate function (preserve the exact predicate logic for "modify", "delete", "add", "clear" actions)
- `onFrameSelectionUpdate(data: FrameSelectionUpdateEvent)` -- calls `ctx.setFrameSelection(data.indices || null)`

Import `useAppStore` from `../../store` for `getState()` calls in onFrameUpdate and onFramesInvalidate (showSnackbar).
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/zndraw-fastapi/frontend && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>connectionHandlers.ts exports factory with onConnect/onDisconnect/onConnectError. handleRoomJoin is a separate internal function. Retry state owned by module. frameHandlers.ts exports factory with onFrameUpdate/onFramesInvalidate/onFrameSelectionUpdate. Query predicate logic preserved exactly. All event parameters typed.</done>
</task>

<task type="auto">
  <name>Task 2: Extract geometryHandlers and roomHandlers</name>
  <files>frontend/src/hooks/socketHandlers/geometryHandlers.ts, frontend/src/hooks/socketHandlers/roomHandlers.ts</files>
  <action>
**geometryHandlers.ts** (from useSocketManager.ts lines 388-394, 527-637, 698-718, 357-360, 630-637):

Export typed interfaces:
- `GeometryInvalidateEvent`: `{ operation?: "set" | "delete"; key?: string }`
- `DefaultCameraInvalidateEvent`: `{ room_id: string; default_camera: string | null }`
- `ActiveCameraUpdateEvent`: `{ active_camera: string }`

`createGeometryHandlers(ctx: HandlerContext)` returns `{ onGeometriesInvalidate, onSelectionsInvalidate, onSelectionGroupsInvalidate, onBookmarksInvalidate, onDefaultCameraInvalidate, onActiveCameraUpdate }`:

- `onGeometriesInvalidate(data: GeometryInvalidateEvent)` -- the most complex handler (~100 lines). Handles "delete" (removeGeometry, removeQueries, invalidateQueries) and "set" (with key: fetch single geometry via `getGeometry`, update store; without key: refetch all geometries). Auto-selects curves via `useAppStore.getState().activeCurveForDrawing`. Import `getGeometry`, `listGeometries` from `../../myapi/client`
- `onSelectionsInvalidate` -- use `createInvalidateHandler` from `./utils`. Fetch via `listGeometries`, extract selections (skip Camera type). Pass `() => ctx.roomId` as getRoomId
- `onSelectionGroupsInvalidate` -- use `createInvalidateHandler` from `./utils`. Fetch via `listSelectionGroups`. Pass `() => ctx.roomId` as getRoomId
- `onBookmarksInvalidate` -- use `createInvalidateHandler` from `./utils`. Fetch via `getAllBookmarks`. Pass `() => ctx.roomId` as getRoomId
- `onDefaultCameraInvalidate(data: DefaultCameraInvalidateEvent)` -- sets query data via `ctx.queryClient.setQueryData(["defaultCamera", data.room_id], ...)`
- `onActiveCameraUpdate(data: ActiveCameraUpdateEvent)` -- reads `setAttachedCameraKey` from `useAppStore.getState()`

Import: `getGeometry`, `listGeometries`, `listSelectionGroups`, `getAllBookmarks` from `../../myapi/client`. `createInvalidateHandler` from `./utils`. `useAppStore` from `../../store` for `.getState()` reads. `type GeometryData` from `../../myapi/client` for the selection extraction type assertion.

**roomHandlers.ts** (from useSocketManager.ts lines 720-796):

Export typed interfaces:
- `RoomUpdateEvent`: `{ id: string; frame_count?: number | null; locked?: boolean | null; [key: string]: unknown }`
- `RoomDeleteEvent`: `{ room_id: string }`
- `LockUpdateEvent`: `{ action: "acquired" | "refreshed" | "released"; user_id?: string | null; sid?: string; msg?: string | null; ttl?: number }`
- `ProgressStartedEvent`: `{ progress_id: string; description: string; unit?: string }`
- `ProgressUpdateEvent`: `{ progress_id: string; n?: number; total?: number | null; elapsed?: number }`
- `ProgressCompleteEvent`: `{ progress_id: string }`

`createRoomHandlers(ctx: HandlerContext)` returns `{ onRoomUpdate, onRoomDelete, onLockUpdate, onProgressStarted, onProgressUpdate, onProgressComplete }`:

- `onRoomUpdate(data: RoomUpdateEvent)` -- updates in-room state if `data.id === ctx.roomId` (frameCount, superuserLock). Upserts into rooms store via `useRoomsStore.getState().setRoom(data.id, data)`
- `onRoomDelete(data: RoomDeleteEvent)` -- removes from rooms store via `useRoomsStore.getState().removeRoom(data.room_id)`
- `onLockUpdate(data: LockUpdateEvent)` -- complex lock logic. Reads `sessionId`, `lockToken`, `startLockExpiryTimer`, `stopLockRenewal`, `stopLockExpiryTimer`, `showSnackbar`, `setState` from `useAppStore.getState()`. Uses `ctx.setUserLock` for remote lock changes
- `onProgressStarted(data: ProgressStartedEvent)` -- calls `ctx.addProgressTracker({...})`
- `onProgressUpdate(data: ProgressUpdateEvent)` -- calls `ctx.updateProgressTracker(data)`
- `onProgressComplete(data: ProgressCompleteEvent)` -- calls `ctx.removeProgressTracker(data.progress_id)`

Import: `useRoomsStore` from `../../roomsStore`. `useAppStore` from `../../store` for `.getState()` imperative reads in onLockUpdate.
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/zndraw-fastapi/frontend && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>geometryHandlers.ts exports factory with 6 handlers. Uses createInvalidateHandler from utils for selections, selectionGroups, bookmarks. roomHandlers.ts exports factory with 6 handlers. Correctly interacts with useRoomsStore and useAppStore via getState(). All event parameters typed with exported interfaces. TypeScript compilation passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes (all 9 socketHandlers files type-check)
- connectionHandlers has handleRoomJoin as separate internal function
- connectionHandlers owns retryDelay/backoff state
- geometryHandlers uses createInvalidateHandler from utils.ts for 3 handlers
- roomHandlers uses useRoomsStore.getState() for room upsert/remove
- No `any` on any event parameter in any handler module
</verification>

<success_criteria>
- 4 files created: connectionHandlers.ts, frameHandlers.ts, geometryHandlers.ts, roomHandlers.ts
- All 7 handler modules now exist across Plans 01 and 02
- Each module exports a createXxxHandlers factory + typed event interfaces
- SOCK-01 (connection), SOCK-02 (frame), SOCK-03 (geometry), SOCK-06 (room) fulfilled
- SOCK-08 (typed interfaces) partially fulfilled (all modules have types)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-socket-handler-modules/03-02-SUMMARY.md`
</output>
