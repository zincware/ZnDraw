---
phase: 03-socket-handler-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/socketHandlers/types.ts
  - frontend/src/hooks/socketHandlers/utils.ts
  - frontend/src/hooks/socketHandlers/chatHandlers.ts
  - frontend/src/hooks/socketHandlers/sceneInvalidationHandlers.ts
  - frontend/src/hooks/socketHandlers/figureHandlers.ts
autonomous: true
requirements: [SOCK-04, SOCK-05, SOCK-08]

must_haves:
  truths:
    - "HandlerContext interface exists with all ~30 dependency fields"
    - "createInvalidateHandler utility is generic and reusable"
    - "Chat handlers own typingTimeouts Map and expose cleanup function"
    - "Scene invalidation handlers correctly use appStoreRoomId for schema queries"
    - "Figure handlers interact with windowManagerStore, not appStore"
    - "All handler event parameters use typed interfaces instead of any"
  artifacts:
    - path: "frontend/src/hooks/socketHandlers/types.ts"
      provides: "HandlerContext interface"
      exports: ["HandlerContext"]
    - path: "frontend/src/hooks/socketHandlers/utils.ts"
      provides: "createInvalidateHandler factory"
      exports: ["createInvalidateHandler"]
    - path: "frontend/src/hooks/socketHandlers/chatHandlers.ts"
      provides: "Chat handler factory with cleanup"
      exports: ["createChatHandlers", "TypingEvent", "ChatHandlersResult"]
    - path: "frontend/src/hooks/socketHandlers/sceneInvalidationHandlers.ts"
      provides: "Scene invalidation handler factory"
      exports: ["createSceneInvalidationHandlers", "InvalidateEvent", "SchemaInvalidateEvent"]
    - path: "frontend/src/hooks/socketHandlers/figureHandlers.ts"
      provides: "Figure handler factory"
      exports: ["createFigureHandlers", "FigureInvalidateEvent"]
  key_links:
    - from: "frontend/src/hooks/socketHandlers/chatHandlers.ts"
      to: "frontend/src/types/chat.ts"
      via: "import MessageNewEvent, MessageEditedEvent"
      pattern: "import.*MessageNewEvent.*MessageEditedEvent.*from.*types/chat"
    - from: "frontend/src/hooks/socketHandlers/figureHandlers.ts"
      to: "frontend/src/stores/windowManagerStore.ts"
      via: "import useWindowManagerStore for getState()"
      pattern: "useWindowManagerStore\\.getState\\(\\)"
    - from: "frontend/src/hooks/socketHandlers/sceneInvalidationHandlers.ts"
      to: "HandlerContext.appStoreRoomId"
      via: "ctx.appStoreRoomId for schema query key"
      pattern: "ctx\\.appStoreRoomId"
---

<objective>
Create the socketHandlers/ foundation (HandlerContext type, createInvalidateHandler utility) and extract the three smallest handler modules: chatHandlers, sceneInvalidationHandlers, and figureHandlers.

Purpose: Establish the directory structure, shared types, and utility that all handler modules depend on. The three smallest modules are extracted first to validate the factory pattern before tackling the larger handlers.

Output: 5 new files under `frontend/src/hooks/socketHandlers/` -- types.ts, utils.ts, chatHandlers.ts, sceneInvalidationHandlers.ts, figureHandlers.ts
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-socket-handler-modules/03-CONTEXT.md
@.planning/phases/03-socket-handler-modules/03-RESEARCH.md
@frontend/src/hooks/useSocketManager.ts
@frontend/src/types/chat.ts
@frontend/src/socket.ts
@frontend/src/store.tsx
@frontend/src/stores/windowManagerStore.ts

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From frontend/src/types/chat.ts:
```typescript
export interface MessageNewEvent {
  id: number; room_id: string; user_id: string; content: string;
  created_at: string; updated_at: string | null; email: string | null;
}
export interface MessageEditedEvent {
  id: number; room_id: string; content: string; updated_at: string;
}
```

From frontend/src/store.tsx:
```typescript
export interface Progress {
  progress_id: string; description: string; n: number;
  total: number | null; elapsed: number; unit: string;
}
export interface InitializationError {
  message: string; details?: string;
}
export type AppState = ConnectionSlice & PlaybackSlice & UISlice & LockSlice & SceneSlice;
export const useAppStore = create<AppState>(...);
```

From frontend/src/stores/windowManagerStore.ts:
```typescript
// Zustand store with openWindow, closeWindow, getState() methods
export const useWindowManagerStore = create<WindowManagerState>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HandlerContext type and createInvalidateHandler utility</name>
  <files>frontend/src/hooks/socketHandlers/types.ts, frontend/src/hooks/socketHandlers/utils.ts</files>
  <action>
Create the `frontend/src/hooks/socketHandlers/` directory with two foundation files.

**types.ts** -- Define the `HandlerContext` interface with all ~30 dependency fields. Derive the exact field list from `useSocketManager.ts` lines 34-73 (the selector block at the top of the hook). Include:

- Identity/routing: `roomId: string | undefined`, `appStoreRoomId: string | null`, `isOverview: boolean`, `isCancelled: () => boolean`
- Query client: `queryClient: QueryClient` (import type from @tanstack/react-query)
- Connection setters: `setConnected`, `setInitializationError`, `setUser`, `setSessionId`, `setCameraKey`, `setServerVersion`, `setGlobalSettings`
- Playback setters: `setFrameCount`, `setCurrentFrame`, `setFrameSelection`, `setBookmarks`
- Scene/geometry setters: `setSelections`, `setSelectionGroups`, `setGeometries`, `setGeometrySchemas`, `setGeometryDefaults`, `updateGeometry`, `removeGeometry`, `setActiveCurveForDrawing`
- Lock setters: `setSuperuserLock`, `setUserLock`
- Progress setters: `setProgressTrackers`, `addProgressTracker`, `updateProgressTracker`, `removeProgressTracker`
- Window manager: `openWindow: (figureKey: string) => void`

Use proper types for all setter functions (match the Zustand slice signatures). Import `type` for `QueryClient`, `GeometryData`, `GlobalSettings`, `UserInfo`, `InitializationError`, `Progress` from their source modules (check actual export locations).

**utils.ts** -- Extract the `createInvalidateHandler` factory from `useSocketManager.ts` lines 85-99. Key change from original: instead of closing over `roomId` from the useEffect, the extracted version receives `getRoomId: () => string | undefined` as a parameter (a getter function). This preserves the same behavior since the orchestrator will pass `() => ctx.roomId`.

```typescript
export function createInvalidateHandler<T>(
  fetchFn: (roomId: string) => Promise<T>,
  updateStoreFn: (data: T) => void,
  eventName: string,
  getRoomId: () => string | undefined,
): (data: unknown) => Promise<void> {
  return async () => {
    const roomId = getRoomId();
    if (!roomId) return;
    try {
      const response = await fetchFn(roomId);
      updateStoreFn(response);
    } catch (error) {
      console.error(`Error fetching ${eventName}:`, error);
    }
  };
}
```
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/zndraw-fastapi/frontend && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>types.ts exports HandlerContext with all ~30 fields using proper types. utils.ts exports createInvalidateHandler with getRoomId parameter. Both files compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extract chatHandlers, sceneInvalidationHandlers, and figureHandlers</name>
  <files>frontend/src/hooks/socketHandlers/chatHandlers.ts, frontend/src/hooks/socketHandlers/sceneInvalidationHandlers.ts, frontend/src/hooks/socketHandlers/figureHandlers.ts</files>
  <action>
Extract three handler modules from `useSocketManager.ts`. Each module exports a `createXxxHandlers(ctx: HandlerContext)` factory returning an object of named handler functions. Each module defines typed interfaces for its socket event payloads (fulfilling SOCK-08). Import `HandlerContext` from `./types`.

**chatHandlers.ts** (from useSocketManager.ts lines 458-525):
- Export `TypingEvent` interface: `{ user_id: string; email: string; is_typing: boolean }`
- Export `ChatHandlersResult` interface: `{ handlers: ChatHandlers; cleanup: () => void }`
- `createChatHandlers(ctx)` returns `ChatHandlersResult`
- Owns the `typingTimeouts` Map internally (mutable state, NOT in HandlerContext)
- `onChatMessageNew(data: MessageNewEvent)` -- import MessageNewEvent from `../../types/chat`. Updates query cache with `ctx.queryClient.setQueryData(["chat", ctx.roomId], ...)`. Reads `chatOpen` and `incrementChatUnread` from `useAppStore.getState()` (imperative, NOT from context -- these change during effect lifetime)
- `onChatMessageUpdated(data: MessageEditedEvent)` -- import MessageEditedEvent from `../../types/chat`. Updates query cache
- `onTyping(data: TypingEvent)` -- reads `addTypingUser`/`removeTypingUser` from `useAppStore.getState()`. Manages typingTimeouts Map
- `cleanup()` -- clears all pending timeouts from typingTimeouts Map

**sceneInvalidationHandlers.ts** (from useSocketManager.ts lines 362-381):
- Export `InvalidateEvent` interface: `{ roomId: string; userName: string; category: string; extension: string; sessionId?: string }`
- Export `SchemaInvalidateEvent` interface: `{ category: string }`
- `createSceneInvalidationHandlers(ctx)` returns `{ onInvalidate, onSchemaInvalidate }`
- `onInvalidate(data: InvalidateEvent)` -- uses `ctx.queryClient.invalidateQueries()` for extension data queries. Also invalidates settings queries when `category === "settings"` and sessionId is present
- `onSchemaInvalidate(data: SchemaInvalidateEvent)` -- uses `ctx.queryClient.invalidateQueries()` with `ctx.appStoreRoomId` (NOT `ctx.roomId` -- this is the existing behavior, must be preserved)

**figureHandlers.ts** (from useSocketManager.ts lines 639-696):
- Export `FigureInvalidateEvent` interface: `{ key: string; operation?: "set" | "delete" }`
- `createFigureHandlers(ctx)` returns `{ onFiguresInvalidate }`
- `onFiguresInvalidate(data: FigureInvalidateEvent)` -- handles both "set" and "delete" operations
- For "delete": removes query cache, closes open windows via `useWindowManagerStore.getState()`, invalidates list
- For "set": invalidates queries, auto-opens window via `ctx.openWindow(data.key)` if not already open and under MAX_AUTO_OPEN_WINDOWS (5)
- Imports `useWindowManagerStore` from `../../stores/windowManagerStore` for `getState()` calls (imperative access, not a hook call)
- Uses `ctx.queryClient` for cache operations, `ctx.roomId` for query keys

Do NOT use `any` for event data parameters -- use the typed interfaces defined in each module. This is the core of SOCK-08.
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/zndraw-fastapi/frontend && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>Three handler modules compile without errors. Each exports a createXxxHandlers factory and typed event interfaces. Chat module returns handlers + cleanup. Scene invalidation uses appStoreRoomId for schema queries. Figure module uses windowManagerStore.getState() for window management. No `any` types on event parameters.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes (all new files type-check against existing codebase)
- Each module exports its factory function and typed event interfaces
- No `any` used for socket event parameters in the new modules
- HandlerContext fields match the selectors in useSocketManager.ts lines 34-69
- createInvalidateHandler uses getRoomId getter pattern
</verification>

<success_criteria>
- 5 files created under frontend/src/hooks/socketHandlers/
- HandlerContext interface has all ~30 fields with proper types
- createInvalidateHandler extracted to utils.ts with getRoomId parameter
- chatHandlers owns typingTimeouts Map and exposes cleanup
- sceneInvalidationHandlers uses appStoreRoomId (not roomId) for schema queries
- figureHandlers uses windowManagerStore.getState() for imperative access
- All event parameters typed (no `any`) -- SOCK-08 partially fulfilled
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-socket-handler-modules/03-01-SUMMARY.md`
</output>
