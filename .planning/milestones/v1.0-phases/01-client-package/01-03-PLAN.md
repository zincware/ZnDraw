---
phase: 01-client-package
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/test_client/__init__.py
  - tests/test_client/test_serialization.py
  - tests/test_client/test_exceptions.py
autonomous: true
requirements: [CLNT-10]

must_haves:
  truths:
    - "Round-trip serialization tests pass with real ASE Atoms objects"
    - "Exception hierarchy smoke tests confirm correct inheritance and instantiation"
    - "All new unit tests pass via uv run pytest tests/test_client/ -x"
  artifacts:
    - path: "tests/test_client/__init__.py"
      provides: "Empty file for pytest subdirectory discovery"
    - path: "tests/test_client/test_serialization.py"
      provides: "Behavioral tests for serialization round-trips"
      min_lines: 30
    - path: "tests/test_client/test_exceptions.py"
      provides: "Smoke tests for exception hierarchy"
      min_lines: 15
  key_links:
    - from: "tests/test_client/test_serialization.py"
      to: "src/zndraw/client/serialization.py"
      via: "direct import of serialization functions"
      pattern: "from zndraw\\.client\\.serialization import"
    - from: "tests/test_client/test_exceptions.py"
      to: "src/zndraw/client/exceptions.py"
      via: "direct import of NotConnectedError"
      pattern: "from zndraw\\.client\\.exceptions import"
    - from: "tests/test_client/test_exceptions.py"
      to: "src/zndraw/exceptions.py"
      via: "direct import of ZnDrawError, RoomLockedError"
      pattern: "from zndraw\\.exceptions import"
---

<objective>
Add unit tests for the extracted client submodules to complete CLNT-10.

Purpose: The existing 499 integration tests verify overall behavior but don't test the extracted modules in isolation. Per user decision: behavioral round-trip tests for serialization helpers using real ASE Atoms from `molify.smiles2atoms`, and smoke tests for the exception hierarchy. No mocked tests for APIManager/SocketManager (integration tests cover that).

Output: tests/test_client/ directory with test_serialization.py and test_exceptions.py, all passing.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-client-package/01-CONTEXT.md
@.planning/phases/01-client-package/01-RESEARCH.md
@.planning/phases/01-client-package/01-02-SUMMARY.md

<interfaces>
<!-- Serialization API to test -->
From src/zndraw/client/serialization.py:
```python
def atoms_to_json_dict(atoms: ase.Atoms, connectivity_threshold: int = 1000) -> dict[str, Any]: ...
def json_dict_to_atoms(data: dict[str, Any]) -> ase.Atoms: ...
def raw_frame_to_atoms(frame: dict[str, Any]) -> ase.Atoms: ...
def _estimate_frame_size(frame: dict[str, str]) -> int: ...
_TARGET_CHUNK_BYTES: int = 2_000_000
_MAX_CHUNK_FRAMES: int = 1000
```

<!-- Exception hierarchy to test -->
From src/zndraw/exceptions.py:
```python
class ZnDrawError(Exception): ...
class RoomLockedError(ZnDrawError): ...
```

From src/zndraw/client/exceptions.py:
```python
class NotConnectedError(ZnDrawError): ...
```

<!-- Test fixture pattern from CONTEXT.md -->
Use `molify.smiles2atoms("O")`, `molify.smiles2atoms("CCO")`, `molify.smiles2atoms("c1ccccc1")` for creating test Atoms fixtures.
Use `pytest.mark.parametrize` per project CLAUDE.md.
Each test must be a function, not a class method.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create serialization round-trip tests</name>
  <files>tests/test_client/__init__.py, tests/test_client/test_serialization.py</files>
  <behavior>
    - Test round-trip: atoms_to_json_dict -> json_dict_to_atoms preserves atom count and positions for water, ethanol, benzene
    - Test round-trip preserves chemical symbols
    - Test _estimate_frame_size returns a positive integer for a non-empty frame
    - Test _estimate_frame_size returns 0 for an empty frame
    - Test atoms_to_json_dict output is a dict with string keys (base64 prefixed)
    - Test connectivity_threshold parameter: atoms below threshold get connectivity, above don't (use a molecule with known atom count)
  </behavior>
  <action>
1. Create `tests/test_client/__init__.py` -- empty file.

2. Create `tests/test_client/test_serialization.py`:
   - Import `pytest`, `molify.smiles2atoms`
   - Import from `zndraw.client.serialization` (direct module path, not via __init__.py -- these are unit tests of the module itself)
   - Use `@pytest.mark.parametrize("smiles", ["O", "CCO", "c1ccccc1"])` for round-trip tests
   - Test: `atoms_to_json_dict` -> `json_dict_to_atoms` preserves `len(atoms)` and `atoms.positions` within tolerance (pytest.approx, abs=1e-6)
   - Test: round-trip preserves `atoms.get_chemical_symbols()`
   - Test: `atoms_to_json_dict` returns a dict with string keys
   - Test: `_estimate_frame_size` returns positive int for frame with content, 0 for empty dict
   - Test: connectivity_threshold parameter -- create atoms with `smiles2atoms("O")` (3 atoms, below default 1000), verify round-trip works; then call with `connectivity_threshold=1` and verify it still works (just skips connectivity)

3. Run `uv run ruff format .` and `uv run ruff check --select I --fix .`
  </action>
  <verify>
    <automated>uv run pytest tests/test_client/test_serialization.py -x -v</automated>
  </verify>
  <done>All serialization round-trip tests pass. Tests use real ASE Atoms from molify.smiles2atoms. Tests use pytest.mark.parametrize.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create exception hierarchy smoke tests</name>
  <files>tests/test_client/test_exceptions.py</files>
  <behavior>
    - Test ZnDrawError is a subclass of Exception
    - Test RoomLockedError is a subclass of ZnDrawError
    - Test NotConnectedError is a subclass of ZnDrawError
    - Test each exception can be instantiated with a message string
    - Test each exception can be raised and caught by its parent type
    - Test RoomLockedError can be caught as ZnDrawError
    - Test NotConnectedError can be caught as ZnDrawError
  </behavior>
  <action>
1. Create `tests/test_client/test_exceptions.py`:
   - Import `pytest`
   - Import `ZnDrawError`, `RoomLockedError` from `zndraw.exceptions`
   - Import `NotConnectedError` from `zndraw.client.exceptions`
   - Test inheritance with `issubclass()` checks
   - Test instantiation: `ZnDrawError("msg")`, `RoomLockedError("msg")`, `NotConnectedError("msg")` -- verify `.args[0] == "msg"`
   - Test catch hierarchy: `with pytest.raises(ZnDrawError): raise RoomLockedError("test")` and same for NotConnectedError
   - Use `@pytest.mark.parametrize` where sensible (e.g., parametrize over exception classes for instantiation test)

2. Run `uv run ruff format .` and `uv run ruff check --select I --fix .`

3. Run full test_client suite: `uv run pytest tests/test_client/ -x -v`
  </action>
  <verify>
    <automated>uv run pytest tests/test_client/test_exceptions.py -x -v</automated>
  </verify>
  <done>Exception hierarchy smoke tests pass. ZnDrawError is base, RoomLockedError and NotConnectedError are subclasses. All can be instantiated and caught by parent type.</done>
</task>

</tasks>

<verification>
All new unit tests pass:
```bash
uv run pytest tests/test_client/ -x -v
```

Full regression still green (final gate):
```bash
uv run pytest tests/ -x
```
</verification>

<success_criteria>
- tests/test_client/ directory exists with __init__.py
- test_serialization.py has round-trip tests using molify.smiles2atoms
- test_exceptions.py has inheritance and instantiation smoke tests
- All new tests pass via `uv run pytest tests/test_client/ -x`
- All 499 existing tests still pass via `uv run pytest tests/ -x`
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-package/01-03-SUMMARY.md`
</output>
